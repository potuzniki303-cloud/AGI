"""Контракт между миром и агентом.

Это единственное место, где мир что-то знает об агенте. Знает он ровно две вещи:
агент умеет выбирать действие и умеет делать свою копию.

Инварианты, которые мир соблюдает и которые НЕ должны нарушаться при доработке:

1. Мир никогда не читает и не пишет веса агента.
2. Мир никогда не вычисляет для агента скаляр «хорошо/плохо» — ни награду,
   ни фитнес, ни счёт.
3. Мир никогда не сортирует и не ранжирует агентов.
4. Размножение — событие физики (энергии хватило на копию), а не операция
   отбора над списком особей.
5. Геном для мира непрозрачен. Мир вызывает spawn() и не знает, что внутри.
6. Статистика в runner.py — только для человека, который смотрит. Она нигде
   не возвращается в симуляцию.
"""

from __future__ import annotations

from typing import Protocol, runtime_checkable

import numpy as np


@runtime_checkable
class Mind(Protocol):
    """Всё, что мир требует от агента.

    Как агент меняет свои веса — его дело. Мир об этом не знает и знать
    не должен: пластичность живёт внутри Mind, а не снаружи.
    """

    def act(self, observation: np.ndarray) -> int:
        """Выбрать действие по наблюдению.

        observation — float32 вектор длины world.observation_size.
        Возврат — индекс действия в [0, world.action_size).

        Стохастичность выбора (если она нужна) — на стороне агента.
        Мир не сэмплирует за агента: иначе часть политики окажется
        в руках дизайнера.
        """
        ...

    def spawn(self, rng: np.random.Generator) -> "Mind":
        """Вернуть потомка.

        Вызывается миром, когда у тела хватило энергии на деление.
        Мутация генома — здесь, потому что копирование с ошибками это
        свойство самого репликатора, а не среды.

        rng передаётся миром, чтобы прогон был воспроизводим.

        ВАЖНО: веса потомка должны быть случайными, а не скопированными
        от родителя. Наследуется правило пластичности, не его результат.
        Если веса наследуются, схема вырождается в обычную нейроэволюцию.
        """
        ...
